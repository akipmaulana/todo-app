import {StyleSheet} from "react-native";
import { Color, Dimen, Font } from "config";

export default StyleSheet.create({
    text_triple_extra_large: {
        fontFamily: Font.family.light,
        fontSize: Font.size.xxx_large,
        flexWrap: 'wrap',
        color: Color.blue,
    }
})